\ProvidesPackage{estilo}

% COMANDOS
%---------------------------------------------------------------------------------------------------------------------------

% Contadores

%Renumerar Partes

%\renewcommand\thepart{\arabic{part}}
%\setcounter{part}{-1}

% Prefixa "\thepart." ao números do capítulo.
%\makeatletter
%\renewcommand{\p@chapter}{\thepart.}
%\makeatother

%Renumerar Capítulos

%\makeatletter
%\@addtoreset{chapter}{part}
%\makeatother

%Renumerar Seções

%\renewcommand\thesection{\arabic{section}}
%\renewcommand\thesubsection{\thesection.\arabic{subsection}}

% PACOTES
%---------------------------------------------------------------------------------------------------------------------------

% Configuração das Páginas

\usepackage[
	a4paper, % Folha.
	%hmargin={2.5cm,2.5cm}, % Margens esquerda (dentro) e direita (fora).
	%vmargin={3cm,3cm} % Margens superior e inferior.
]{geometry} % Pacote que configura as dimensões da página e do texto.
\usepackage{graphicx}
\usepackage[brazilian]{babel} % (Língua em que ambientes são escritos no documento.)

% Configurações do Arquivo .TEX
\usepackage[utf8]{inputenc} %(Codificação em que o arquivo de texto é escrito.)

% Codificação de Fonte (configurações do arquivo .PDF)
\usepackage{lmodern} % Não tenho certeza de como funciona, mas muda a fonte ligeiramente. Resolveu alguns problemas, como tamanho da fonte na capa que ficava muito pequeno, as linhas do .PDF quando grifadas não ficam mais grandes demais e alguns caracteres como o trvessão --- agora podem ser copiados do arquivo .PDF final, antes eram exibidos mas não eram copiáveis.
\usepackage[T1]{fontenc} %(Língua em que o arquivo imagem é feito. Necessário para poder copiar acentos do .pdf.)
\usepackage{textcomp} % Não exatamente o que faz mas acho que melhora a fonte de alguns caracteres.

\usepackage[pagebackref,pdftex,hidelinks]{hyperref}
%\hypersetup{
%  colorlinks   = true,
%  urlcolor     = .,
%  linkcolor    = red,
%  citecolor   = green
%}

% Pacotes de Edição de Texto e Ferramentas Básicas

\usepackage{enumitem}
%\SetEnumitemKey{prop}{ }
%\makeatletter
%\newcommand{\mylabel}[2]{#2\def\@currentlabel{#2}\label{#1}}
%\makeatother
% https://tex.stackexchange.com/questions/253910/reference-to-enumerate-item-with-manually-set-label#253912

\usepackage{caption} % Permite legenda da figura não aparecer.
\usepackage{verbatim}
\usepackage{boxedminipage}
\usepackage{xcolor}
\usepackage{eso-pic}
\usepackage{ccicons}
\usepackage{scrextend} % Listas e ambiente \labelling.

% Matemática

\usepackage{matematica}